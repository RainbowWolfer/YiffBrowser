<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:base="RW.Base.WPF"
	xmlns:c="clr-namespace:YiffBrowser.BaseFramework.Controls"
	xmlns:controlzEx="urn:controlzex"
	xmlns:conv="clr-namespace:YiffBrowser.BaseFramework.Converters"
	xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
	xmlns:e="clr-namespace:YiffBrowser.BaseFramework.Extensions"
	xmlns:hc="https://handyorg.github.io/handycontrol"
	xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
	xmlns:sw="clr-namespace:System.Windows;assembly=PresentationFramework"
	xmlns:sys="clr-namespace:System;assembly=mscorlib"
	xmlns:system="clr-namespace:System;assembly=mscorlib"
	xmlns:views="clr-namespace:YiffBrowser.BaseFramework.Views"
	xmlns:vm="clr-namespace:YiffBrowser.BaseFramework.ViewModels"
	xmlns:vms="clr-namespace:YiffBrowser.BaseFramework.ViewModelServices"
	xmlns:wpf="RW.Common.WPF">


	<ResourceDictionary.MergedDictionaries>
		<ResourceDictionary Source="pack://application:,,,/RW.Common.WPF;component/Resources/Generic.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.Resources;component/ResourcesGeneric.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/NativeControls.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomButton.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomToggleButton.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomScrollViewer.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomMenu.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomListBox.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomPagination.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/CustomDataGrid.xaml"/>
		<ResourceDictionary Source="/YiffBrowser.BaseFramework;component/Resources/PlaybackControls.xaml"/>
	</ResourceDictionary.MergedDictionaries>


	<x:Array x:Key="StringArray1" Type="{x:Type sys:String}">
		<sys:String>In amet at</sys:String>
		<sys:String>Dolores lorem takimata</sys:String>
		<sys:String>Ut ipsum erat</sys:String>
	</x:Array>


	<conv:PassThroughConverter x:Key="PassThroughConverter"/>
	<conv:NumberToKBConverter x:Key="NumberToKBConverter"/>
	<conv:SubStringConverter x:Key="SubStringConverter"/>
	<conv:TimeDurationConverter x:Key="TimeDurationConverter"/>
	<conv:PlaybackTickToTimeSpanConverter x:Key="PlaybackTickToTimeSpanConverter"/>
	<conv:EnumToBoolConverter x:Key="EnumToBoolConverter"/>

	<Style TargetType="TextBlock">
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
	</Style>

	<Style x:Key="TextBlockIcon" TargetType="TextBlock">
		<Setter Property="FontFamily" Value="{StaticResource IconPack}"/>
		<Setter Property="FontSize" Value="16"/>
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
	</Style>

	<Style x:Key="FontIconBase" TargetType="wpf:FontIcon" BasedOn="{StaticResource TextBlockIcon}">
		<Setter Property="FontFamily" Value="{StaticResource IconPack}"/>
		<Setter Property="FontSize" Value="16"/>
		<Setter Property="TextAlignment" Value="Center"/>
		<Setter Property="HorizontalAlignment" Value="Center"/>
		<Setter Property="VerticalAlignment" Value="Center"/>
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
	</Style>

	<Style TargetType="wpf:FontIcon" BasedOn="{StaticResource FontIconBase}"/>

	<ExponentialEase x:Key="DefaultEasingFunction" EasingMode="EaseOut" Exponent="8"/>

	<Style TargetType="ContentControl">
		<Setter Property="Focusable" Value="False"/>
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
	</Style>

	<Style TargetType="Menu" BasedOn="{StaticResource Menu.Small}"/>
	<Style TargetType="ContextMenu" BasedOn="{StaticResource ContextMenu.Custom}"/>

	<Style x:Key="TitleContent.Custom" TargetType="wpf:TitleContent" BasedOn="{StaticResource TitleContentBaseStyle}">
		<Setter Property="LeftTitlePadding" Value="0,7,6,0"/>
		<Setter Property="TopTitlePadding" Value="8,0,0,5"/>
		<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
	</Style>

	<Style TargetType="wpf:TitleContent" BasedOn="{StaticResource TitleContent.Custom}"/>

	<Style TargetType="c:BoolContent">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:BoolContent">
					<Border
						Padding="{TemplateBinding Padding}"
						BorderThickness="{TemplateBinding BorderThickness}"
						BorderBrush="{TemplateBinding BorderBrush}"
						Background="{TemplateBinding Background}">
						<Grid>
							<ContentControl Name="TrueContent" Content="{TemplateBinding TrueContent}"/>
							<ContentControl Name="FalseContent" Content="{TemplateBinding FalseContent}"/>
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="Bool" Value="True">
							<Setter TargetName="TrueContent" Property="Visibility" Value="Visible"/>
							<Setter TargetName="FalseContent" Property="Visibility" Value="Collapsed"/>
						</Trigger>
						<Trigger Property="Bool" Value="False">
							<Setter TargetName="TrueContent" Property="Visibility" Value="Collapsed"/>
							<Setter TargetName="FalseContent" Property="Visibility" Value="Visible"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="HorizontalSeparator" TargetType="Border">
		<Setter Property="Background" Value="Gray"/>
		<Setter Property="Width" Value="2"/>
		<Setter Property="Height" Value="10"/>
		<Setter Property="CornerRadius" Value="1"/>
		<Setter Property="Margin" Value="4,0"/>
	</Style>

	<Style x:Key="VerticalSeparator" TargetType="Border">
		<Setter Property="Background" Value="Gray"/>
		<Setter Property="Width" Value="10"/>
		<Setter Property="Height" Value="2"/>
		<Setter Property="CornerRadius" Value="1"/>
		<Setter Property="Margin" Value="0,4"/>
	</Style>

	<Style x:Key="CustomHorizontalPanel" TargetType="wpf:SpacingStackPanel">
		<Setter Property="Orientation" Value="Horizontal"/>
		<Setter Property="HorizontalSpacing" Value="2"/>
		<Setter Property="ItemVerticalAlignment" Value="Center"/>
	</Style>

	<Style x:Key="CustomHorizontalPanel2" TargetType="wpf:SpacingStackPanel">
		<Setter Property="Orientation" Value="Horizontal"/>
		<Setter Property="HorizontalSpacing" Value="4"/>
		<Setter Property="ItemVerticalAlignment" Value="Center"/>
	</Style>

	<Style x:Key="CustomHorizontalPanel3" TargetType="wpf:SpacingStackPanel">
		<Setter Property="Orientation" Value="Horizontal"/>
		<Setter Property="HorizontalSpacing" Value="6"/>
		<Setter Property="ItemVerticalAlignment" Value="Center"/>
	</Style>

	<Style x:Key="CustomVerticalPanel" TargetType="wpf:SpacingStackPanel">
		<Setter Property="Orientation" Value="Vertical"/>
		<Setter Property="VerticalSpacing" Value="2"/>
	</Style>

	<Style x:Key="CustomVerticalPanel2" TargetType="wpf:SpacingStackPanel">
		<Setter Property="Orientation" Value="Vertical"/>
		<Setter Property="VerticalSpacing" Value="4"/>
	</Style>

	<Style x:Key="CustomVerticalPanel3" TargetType="wpf:SpacingStackPanel">
		<Setter Property="Orientation" Value="Vertical"/>
		<Setter Property="VerticalSpacing" Value="6"/>
	</Style>


	<Style x:Key="HorizontalIconTextBaseStyle" TargetType="c:HorizontalIconText">
		<Setter Property="Focusable" Value="False"/>
		<Setter Property="IconForeground" Value="{DynamicResource PrimaryTextBrush}"/>
		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:HorizontalIconText">
					<Border
						Padding="{TemplateBinding Padding}"
						BorderThickness="{TemplateBinding BorderThickness}"
						BorderBrush="{TemplateBinding BorderBrush}"
						Background="{TemplateBinding Background}">
						<DockPanel>
							<wpf:FontIcon Name="PART_IconText"
								VerticalAlignment="Center"
								FontSize="{TemplateBinding IconSize}"
								Foreground="{TemplateBinding IconForeground}"
								Text="{TemplateBinding Glyph}"
								Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Glyph, Converter={StaticResource String2VisibilityConverter}}"/>
							<Border Width="{TemplateBinding Spacing}" Visibility="{Binding ElementName=PART_IconText, Path=Visibility}"/>
							<TextBlock
								VerticalAlignment="Center"
								FontSize="{TemplateBinding FontSize}"
								Foreground="{TemplateBinding Foreground}"
								Text="{TemplateBinding Text}"
								TextAlignment="{TemplateBinding TextAlignment}"
								Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Text, Converter={StaticResource String2VisibilityConverter}}"/>
						</DockPanel>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="c:HorizontalIconText" BasedOn="{StaticResource HorizontalIconTextBaseStyle}"/>

	<Style x:Key="ButtonPopupBorder" TargetType="Border">
		<Setter Property="Focusable" Value="False"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
		<Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
		<Setter Property="Padding" Value="4"/>
		<Setter Property="Margin" Value="10"/>
		<Setter Property="CornerRadius" Value="4"/>
		<Setter Property="UseLayoutRounding" Value="True"/>
		<Setter Property="Effect">
			<Setter.Value>
				<DropShadowEffect
					BlurRadius="4"
					Opacity="0.4"
					ShadowDepth="4"
					Color="Gray"/>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="c:ImageViewer">
		<Setter Property="Focusable" Value="False"/>
		<Setter Property="Background" Value="Transparent"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:ImageViewer">
					<Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}">
						<Grid x:Name="PART_PanelMain" ClipToBounds="True">
							<Border
								Width="{TemplateBinding ImageWidth}"
								Height="{TemplateBinding ImageHeight}"
								Margin="{TemplateBinding ImageMargin}"
								HorizontalAlignment="Left"
								VerticalAlignment="Top">
								<c:ImageExtend x:Name="PART_ImageMain"
									BitmapImage="{TemplateBinding BitmapImage}"
									Focusable="False"
									GifImage="{TemplateBinding GifImage}"
									RenderOptions.BitmapScalingMode="NearestNeighbor"
									RenderTransformOrigin="0.5,0.5"/>
							</Border>
						</Grid>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>


	<!--#region Styles-->

	<Style x:Key="Button.Custom.Icon" TargetType="Button" BasedOn="{StaticResource ButtonDefault}">
		<Setter Property="Padding" Value="0"/>
	</Style>

	<Style x:Key="DialogBackgroundBorder" TargetType="Border">
		<Setter Property="Margin" Value="4,4,4,0"/>
		<Setter Property="CornerRadius" Value="4"/>
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
	</Style>

	<Style x:Key="WindowBaseStyle" TargetType="c:WindowBase" BasedOn="{StaticResource {x:Type controlzEx:WindowChromeWindow}}">
		<Setter Property="Icon" Value="{DynamicResource YiffBrowserIcon_PNG}"/>
		<Setter Property="controlzEx:WindowBackdropManager.BackdropType" Value="Mica"/>
		<Setter Property="UseNativeCaptionButtons" Value="True"/>
		<Setter Property="CaptionColor" Value="Transparent"/>
		<Setter Property="CornerPreference" Value="Round"/>
		<Setter Property="NCActiveBrush" Value="Transparent"/>
		<Setter Property="NCNonActiveBrush" Value="Transparent"/>
		<Setter Property="GlowColor" Value="{DynamicResource BorderColor}"/>
		<Setter Property="NonActiveGlowColor" Value="{DynamicResource BorderColor}"/>

		<Setter Property="SnapsToDevicePixels" Value="True"/>
		<Setter Property="UseLayoutRounding" Value="True"/>

		<Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>

		<!--<Setter Property="Focusable" Value="True"/>
		<Setter Property="IsTabStop" Value="False"/>
		<Setter Property="FocusVisualStyle" Value="{x:Null}"/>-->

	</Style>

	<Style TargetType="c:WindowBase" BasedOn="{StaticResource WindowBaseStyle}"/>

	<Style x:Key="WindowContentStyle" TargetType="c:WindowContent">

		<Setter Property="Focusable" Value="False"/>
		<Setter Property="IsTabStop" Value="False"/>

		<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
		<Setter Property="VerticalContentAlignment" Value="Stretch"/>
		<Setter Property="Background" Value="{DynamicResource RegionBrush}"/>

		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:WindowContent">

					<Grid SnapsToDevicePixels="True">
						<Grid.RowDefinitions>
							<RowDefinition Height="30"/>
							<RowDefinition Height="*"/>
						</Grid.RowDefinitions>

						<Grid Grid.Row="0" controlzEx:NonClientControlProperties.HitTestResult="CAPTION" controlzEx:WindowChrome.IsHitTestVisibleInChrome="False">
							<Grid.ColumnDefinitions>
								<ColumnDefinition/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>

							<Grid Grid.Column="0" Background="Transparent">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition/>
								</Grid.ColumnDefinitions>
								<Button Name="ButtonIcon"
									Style="{StaticResource ButtonCustom}"
									Margin="10,0,0,0"
									VerticalAlignment="Center"
									controlzEx:WindowChrome.IsHitTestVisibleInChrome="True"
									Background="Transparent">
									<Image
										Width="16"
										Height="16"
										VerticalAlignment="Center"
										IsHitTestVisible="False"
										Source="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Icon}"/>
								</Button>
								<TextBlock
									Grid.Column="1"
									Padding="10,0,0,0"
									VerticalAlignment="Center"
									Foreground="{DynamicResource PrimaryTextBrush}"
									Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}"/>
							</Grid>

						</Grid>

						<Border Grid.Row="1" Background="{TemplateBinding Background}">
							<ContentPresenter
								HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
								VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
								Content="{TemplateBinding Content}"
								ContentStringFormat="{TemplateBinding ContentStringFormat}"
								ContentTemplate="{TemplateBinding ContentTemplate}"
								RecognizesAccessKey="True"/>
						</Border>
					</Grid>

				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="c:WindowContent" BasedOn="{StaticResource WindowContentStyle}"/>

	<Style TargetType="c:DialogWindowWrapper">
		<Setter Property="Padding" Value="0"/>
		<Setter Property="IsTabStop" Value="False"/>
		<Setter Property="Focusable" Value="False"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:DialogWindowWrapper">
					<c:WindowContent>
						<Border
							Padding="{TemplateBinding Padding}"
							BorderThickness="{TemplateBinding BorderThickness}"
							BorderBrush="{TemplateBinding BorderBrush}"
							Background="{TemplateBinding Background}">
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="*"/>
									<RowDefinition Height="auto"/>
								</Grid.RowDefinitions>

								<wpf:ClippingBorder Grid.Row="0" Padding="{TemplateBinding Padding}">
									<ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"/>
								</wpf:ClippingBorder>

								<Border Grid.Row="1">
									<ItemsControl
										Margin="4"
										HorizontalAlignment="Right"
										Focusable="False"
										IsTabStop="False"
										ItemsSource="{TemplateBinding DialogCommands}">

										<ItemsControl.ItemsPanel>
											<ItemsPanelTemplate>
												<StackPanel Orientation="Horizontal"/>
											</ItemsPanelTemplate>
										</ItemsControl.ItemsPanel>

										<ItemsControl.ItemTemplateSelector>
											<c:DialogWindowWrapperButtonTemplateSelector>

												<c:DialogWindowWrapperButtonTemplateSelector.Default>
													<DataTemplate DataType="{x:Type vm:DialogCommand}">
														<Button
															Style="{StaticResource ButtonDefault}"
															MinWidth="80"
															Margin="4"
															Command="{Binding RelativeSource={RelativeSource AncestorType=c:DialogWindowWrapper}, Path=DialogButtonCommand}"
															CommandParameter="{Binding}"
															Content="{Binding Content}"
															IsEnabled="{Binding IsEnabled}"/>
													</DataTemplate>
												</c:DialogWindowWrapperButtonTemplateSelector.Default>

												<c:DialogWindowWrapperButtonTemplateSelector.Primary>
													<DataTemplate DataType="{x:Type vm:DialogCommand}">
														<Button
															Style="{StaticResource ButtonPrimary}"
															MinWidth="80"
															Margin="4"
															Command="{Binding RelativeSource={RelativeSource AncestorType=c:DialogWindowWrapper}, Path=DialogButtonCommand}"
															CommandParameter="{Binding}"
															Content="{Binding Content}"
															IsEnabled="{Binding IsEnabled}"/>
													</DataTemplate>
												</c:DialogWindowWrapperButtonTemplateSelector.Primary>

											</c:DialogWindowWrapperButtonTemplateSelector>
										</ItemsControl.ItemTemplateSelector>

									</ItemsControl>
								</Border>
							</Grid>
						</Border>
					</c:WindowContent>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!--#endregion-->




	<!--#region Interation Styles-->
	<Style TargetType="c:TextBoxExtend" BasedOn="{StaticResource TextBoxExtend}"/>

	<Style x:Key="DialogStyle" TargetType="UserControl">
		<Setter Property="Padding" Value="4,4,4,0"/>
		<Setter Property="dxmvvm:Interaction.BehaviorsTemplate">
			<Setter.Value>
				<DataTemplate DataType="{x:Type vm:IDialogViewModel}">
					<ItemsControl>
						<dxmvvm:EventToCommand Command="{Binding LoadedCommand}" EventName="Loaded" PassEventArgsToCommand="True"/>
						<base:DispatcherServiceEx/>
						<vms:MessageBoxServiceEx/>
						<base:TypedUIObjectService Name="DialogUserControlService"/>
					</ItemsControl>
				</DataTemplate>
			</Setter.Value>
		</Setter>
	</Style>


	<!--#endregion-->


	<BorderGapMaskConverter x:Key="BorderGapMaskConverter"/>

	<Style x:Key="GroupBox.Custom" TargetType="{x:Type GroupBox}">
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
		<Setter Property="BorderThickness" Value="1"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type GroupBox}">
					<Grid SnapsToDevicePixels="true">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="6"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="6"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="*"/>
							<RowDefinition Height="6"/>
						</Grid.RowDefinitions>
						<Border
							Grid.Row="1"
							Grid.RowSpan="3"
							Grid.Column="0"
							Grid.ColumnSpan="4"
							BorderThickness="{TemplateBinding BorderThickness}"
							BorderBrush="Transparent"
							Background="{TemplateBinding Background}"
							CornerRadius="4"/>
						<Border
							Grid.Row="1"
							Grid.RowSpan="3"
							Grid.ColumnSpan="4"
							BorderThickness="{TemplateBinding BorderThickness}"
							BorderBrush="Transparent"
							CornerRadius="4">
							<Border.OpacityMask>
								<MultiBinding Converter="{StaticResource BorderGapMaskConverter}" ConverterParameter="7">
									<Binding Path="ActualWidth" ElementName="Header"/>
									<Binding Path="ActualWidth" RelativeSource="{RelativeSource Mode=Self}"/>
									<Binding Path="ActualHeight" RelativeSource="{RelativeSource Mode=Self}"/>
								</MultiBinding>
							</Border.OpacityMask>
							<Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="3">
								<Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="Transparent" CornerRadius="2"/>
							</Border>
						</Border>
						<Border x:Name="Header"
							Grid.Row="0"
							Grid.RowSpan="2"
							Grid.Column="1"
							Padding="3,1,3,0">
							<ContentPresenter ContentSource="Header" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
						</Border>
						<ContentPresenter
							Grid.Row="2"
							Grid.Column="1"
							Grid.ColumnSpan="2"
							Margin="{TemplateBinding Padding}"
							SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="GroupBox" BasedOn="{StaticResource GroupBox.Custom}"/>

	<Style x:Key="SelectableTextBlock" TargetType="TextBox" BasedOn="{StaticResource TextBoxExtend}">
		<Setter Property="Background" Value="Transparent"/>
		<Setter Property="BorderThickness" Value="0"/>
		<Setter Property="Padding" Value="0"/>
		<Setter Property="IsReadOnly" Value="True"/>
		<Setter Property="VerticalContentAlignment" Value="Top"/>
		<Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
		<Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
	</Style>

	<Style x:Key="FolderSelectionControl" TargetType="c:FolderSelectionControl">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="c:FolderSelectionControl">
					<Border>
						<hc:ElementGroup>
							<hc:ElementGroup.ItemsPanel>
								<ItemsPanelTemplate>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="Auto"/>
											<ColumnDefinition Width="Auto"/>
										</Grid.ColumnDefinitions>
									</Grid>
								</ItemsPanelTemplate>
							</hc:ElementGroup.ItemsPanel>

							<hc:TextBox
								HorizontalAlignment="Stretch"
								DockPanel.Dock="Left"
								IsReadOnly="True"
								Text="{TemplateBinding Path}"/>

							<Button x:Name="PART_ButtonEdit"
								Style="{StaticResource ButtonDefault}"
								Grid.Column="1"
								ToolTip="Edit"
								Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsReadonly, Converter={StaticResource Boolean2VisibilityReConverter}}">
								<wpf:FontIcon Text="&#xE70F;"/>
							</Button>

							<Button x:Name="PART_ButtonShow"
								Style="{StaticResource ButtonDefault}"
								Grid.Column="2"
								ToolTip="Show">
								<wpf:FontIcon Text="&#xE838;"/>
							</Button>

						</hc:ElementGroup>
					</Border>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style TargetType="c:FolderSelectionControl" BasedOn="{StaticResource FolderSelectionControl}"/>



	<Style x:Key="ProgressBar.Custom" TargetType="ProgressBar">
		<Setter Property="hc:VisualElement.Text">
			<Setter.Value>
				<MultiBinding Converter="{StaticResource Number2PercentageConverter}" StringFormat="{}{0:F0} %">
					<Binding Path="Value" RelativeSource="{RelativeSource Self}"/>
					<Binding Path="Maximum" RelativeSource="{RelativeSource Self}"/>
				</MultiBinding>
			</Setter.Value>
		</Setter>
		<Setter Property="Height" Value="20"/>
		<Setter Property="hc:BorderElement.CornerRadius" Value="{StaticResource DefaultCornerRadius}"/>
		<Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}"/>
		<Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
		<Setter Property="BorderThickness" Value="0"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ProgressBar">
					<ControlTemplate.Resources>
						<Storyboard x:Key="IsIndeterminate" RepeatBehavior="Forever">
							<DoubleAnimationUsingKeyFrames Storyboard.TargetName="Animation" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
								<EasingDoubleKeyFrame KeyTime="0" Value="0.25"/>
								<EasingDoubleKeyFrame KeyTime="0:0:1" Value="0.25"/>
								<EasingDoubleKeyFrame KeyTime="0:0:2" Value="0.25"/>
							</DoubleAnimationUsingKeyFrames>
							<PointAnimationUsingKeyFrames Storyboard.TargetName="Animation" Storyboard.TargetProperty="(UIElement.RenderTransformOrigin)">
								<EasingPointKeyFrame KeyTime="0" Value="-0.5,0.5"/>
								<EasingPointKeyFrame KeyTime="0:0:1" Value="0.5,0.5"/>
								<EasingPointKeyFrame KeyTime="0:0:2" Value="1.5,0.5"/>
							</PointAnimationUsingKeyFrames>
						</Storyboard>
					</ControlTemplate.Resources>
					<Border x:Name="TemplateRoot"
						BorderThickness="{TemplateBinding BorderThickness}"
						BorderBrush="{TemplateBinding BorderBrush}"
						Background="{TemplateBinding Background}"
						CornerRadius="{Binding Path=(hc:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
						<hc:SimplePanel>
							<Rectangle x:Name="PART_Track"/>
							<!--<TextBlock
								HorizontalAlignment="Center"
								VerticalAlignment="Center"
								Foreground="{TemplateBinding Foreground}"
								Text="{Binding Path=(hc:VisualElement.Text), RelativeSource={RelativeSource TemplatedParent}}"
								Visibility="{Binding Visibility, ElementName=PART_GlowRect}"/>-->
							<Border x:Name="PART_Indicator"
								Style="{StaticResource BorderClip}"
								HorizontalAlignment="Left"
								CornerRadius="{Binding Path=(hc:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
								<hc:SimplePanel>
									<Border x:Name="Indicator" Background="{TemplateBinding Foreground}">
										<Border Width="{Binding ActualWidth, ElementName=TemplateRoot}">
											<!--<TextBlock
												HorizontalAlignment="Center"
												VerticalAlignment="Center"
												Foreground="{DynamicResource TextIconBrush}"
												Text="{Binding Path=(hc:VisualElement.Text), RelativeSource={RelativeSource TemplatedParent}}"/>-->
										</Border>
									</Border>
									<Rectangle x:Name="PART_GlowRect"
										Width="100"
										Margin="-100,0,0,0"
										HorizontalAlignment="Left"
										Fill="{StaticResource ProgressBarIndicatorAnimatedFill}"/>
									<Rectangle x:Name="Animation"
										Fill="{TemplateBinding Foreground}"
										RenderTransformOrigin="0.5,0.5"
										Visibility="Collapsed">
										<Rectangle.RenderTransform>
											<TransformGroup>
												<ScaleTransform/>
											</TransformGroup>
										</Rectangle.RenderTransform>
									</Rectangle>
								</hc:SimplePanel>
							</Border>
						</hc:SimplePanel>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="Orientation" Value="Vertical">
							<Setter Property="Height" Value="{x:Static system:Double.NaN}"/>
							<Setter TargetName="TemplateRoot" Property="LayoutTransform">
								<Setter.Value>
									<RotateTransform Angle="-90"/>
								</Setter.Value>
							</Setter>
						</Trigger>
						<Trigger Property="IsIndeterminate" Value="true">
							<Trigger.EnterActions>
								<BeginStoryboard Storyboard="{StaticResource IsIndeterminate}"/>
							</Trigger.EnterActions>
							<Setter TargetName="Indicator" Property="Visibility" Value="Collapsed"/>
							<Setter TargetName="PART_GlowRect" Property="Visibility" Value="Collapsed"/>
							<Setter TargetName="Animation" Property="Visibility" Value="Visible"/>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>


</ResourceDictionary>